/**
 * Página placeholder de leads.
 * A implementação completa (tabela + toolbar + export CSV) será feita no Ticket #11.
 */
export function AdminLeads() {
  return (
    <div className="p-6">
      <h2 className="text-2xl font-bold mb-2">Leads</h2>
      <p className="text-gray-600">Gestão de leads em breve.</p>
    </div>
  );
}
